# 🔍 Отладка TTS (Yandex)

## Что исправлено:

1. ✅ Исправлены URL в `textToSpeechAdvanced.ts` - теперь используются полные пути к бэкенду
2. ✅ Добавлено подробное логирование для отладки

## Как проверить:

### 1. Откройте консоль браузера (F12)

### 2. Откройте сессию и начните диалог

### 3. Смотрите логи в консоли:

**При инициализации должно быть:**
```
🎙️ TTS инициализирован: { provider: 'yandex', language: 'ru', voice: 'jane' }
```

**При отправке сообщения:**
```
🔊 TTS speak: провайдер=yandex, язык=ru, текст="..."
🔊 Используется Yandex TTS
🎤 Yandex TTS: отправка запроса на http://localhost:8080/api/tts/yandex
🎤 Текст: ...
🎤 Голос: jane
🎤 Yandex TTS: статус ответа 200 OK
✅ Yandex TTS: аудио получено, начинаем воспроизведение
```

## Возможные проблемы:

### ❌ Проблема 1: Используется Browser TTS
**Логи:**
```
🔊 Используется Browser TTS
```

**Решение:**
- Проверьте, что `ttsProvider` в `SessionView.tsx` установлен в `'yandex'`
- Убедитесь, что при создании `TextToSpeechAdvanced` передается `provider: 'yandex'`

### ❌ Проблема 2: Ошибка 401 (Unauthorized)
**Логи:**
```
❌ Yandex TTS failed: 401
```

**Решение:**
- Проверьте, что токен JWT есть в `localStorage.getItem('token')`
- Убедитесь, что вы залогинены

### ❌ Проблема 3: Ошибка 400 или 500
**Логи:**
```
❌ Yandex TTS failed: 400 - ...
```

**Решение:**
- Проверьте логи бэкенда - там будет подробная ошибка
- Убедитесь, что Yandex API ключ правильно настроен в `application.yml`

### ❌ Проблема 4: Fallback на OpenAI/Browser
**Логи:**
```
Yandex TTS error: ...
Falling back to OpenAI TTS...
```

**Решение:**
- Проверьте, что Yandex API ключ правильный
- Проверьте, что бэкенд может достучаться до Yandex API (интернет, файрвол)

## Проверка на бэкенде:

### Логи бэкенда должны показывать:
```
Yandex TTS: запрос получен
Yandex TTS: синтез начат
Yandex TTS: аудио отправлено
```

### Если ошибка:
```
Yandex TTS error: ...
```

Проверьте:
- Правильность API ключа в `application.yml`
- Доступность Yandex API (может быть блокировка по IP)

---

## После исправлений:

1. **Перезагрузите страницу** (Ctrl+R или Cmd+R)
2. **Откройте сессию заново**
3. **Отправьте сообщение**
4. **Смотрите логи в консоли**

Голос должен быть **естественным и выразительным** (Yandex), а не роботизированным (Browser TTS)!

---

## Если все равно не работает:

Пришлите логи из консоли браузера и логи бэкенда, и я помогу разобраться! 🔧

