# 🤖 OpenAI API - Настройки и параметры

## 📋 Доступные настройки

Все настройки находятся в `application.yml` в секции `openai:`:

### 🔑 Основные настройки

#### `api-key`
- **Описание**: API ключ OpenAI
- **Тип**: String
- **Пример**: `sk-proj-...`
- **Безопасность**: ⚠️ НЕ коммитьте в Git! Используйте переменные окружения

#### `model`
- **Описание**: Модель GPT для использования
- **Варианты**:
  - `gpt-4-turbo-preview` - Самая мощная, быстрая (рекомендуется)
  - `gpt-4` - Классическая GPT-4
  - `gpt-4o` - GPT-4 Optimized (быстрая и дешевая)
  - `gpt-4o-mini` - Упрощенная версия GPT-4o (самая дешевая)
  - `gpt-3.5-turbo` - Быстрая и дешевая, но менее умная
- **По умолчанию**: `gpt-4-turbo-preview`
- **Стоимость**: GPT-4 дороже, GPT-3.5 дешевле

---

### 📏 Длина ответов

#### `max-tokens`
- **Описание**: Максимальная длина ответа в токенах
- **Тип**: Integer
- **Формула**: 1 токен ≈ 0.75 слова (русский) или 0.5 слова (английский)
- **Рекомендации**:
  - **Короткие ответы**: `500-1000` токенов (375-750 слов)
  - **Средние ответы**: `1000-2000` токенов (750-1500 слов) ✅ **Рекомендуется**
  - **Длинные ответы**: `2000-4000` токенов (1500-3000 слов)
  - **Очень длинные**: `4000-8000` токенов (максимум для большинства моделей)
- **По умолчанию**: `2000`
- **Пример**:
  ```yaml
  max-tokens: 1500  # Для более коротких ответов
  max-tokens: 3000  # Для детальных объяснений
  ```

---

### 🎨 Креативность и точность

#### `temperature`
- **Описание**: Контролирует случайность/креативность ответов
- **Тип**: Double (0.0 - 2.0)
- **Значения**:
  - `0.0` - Детерминированный, всегда одинаковый ответ на один вопрос
  - `0.1-0.3` - Очень точный, предсказуемый
  - `0.7` - ✅ **Рекомендуется** - Баланс креативности и точности
  - `1.0-1.5` - Креативный, разнообразный
  - `2.0` - Максимально креативный, непредсказуемый
- **По умолчанию**: `0.7`
- **Для обучения**: `0.3-0.7` (нужна точность)
- **Для творчества**: `0.8-1.2`

#### `top-p` (Nucleus Sampling)
- **Описание**: Альтернатива temperature, контролирует разнообразие через вероятности
- **Тип**: Double (0.0 - 1.0)
- **Значения**:
  - `0.1` - Использует только 10% самых вероятных токенов (очень предсказуемо)
  - `0.9` - ✅ **Рекомендуется** - Использует 90% вероятностной массы
  - `1.0` - Использует все возможные токены
- **По умолчанию**: `1.0`
- **Примечание**: Используйте либо `temperature`, либо `top-p`, не оба одновременно

---

### 🔄 Контроль повторений

#### `frequency-penalty`
- **Описание**: Штраф за повторение токенов
- **Тип**: Double (-2.0 - 2.0)
- **Значения**:
  - `-2.0` - Поощряет повторения
  - `0.0` - ✅ **По умолчанию** - Нет штрафа
  - `0.5-1.0` - Умеренный штраф за повторения
  - `2.0` - Максимальный штраф (избегает повторений)
- **Использование**: Если AI повторяет фразы, увеличьте до `0.5-1.0`

#### `presence-penalty`
- **Описание**: Штраф за упоминание уже упомянутых тем
- **Тип**: Double (-2.0 - 2.0)
- **Значения**:
  - `-2.0` - Поощряет возврат к старым темам
  - `0.0` - ✅ **По умолчанию** - Нет штрафа
  - `0.5-1.0` - Поощряет новые темы
  - `2.0` - Максимально поощряет новые темы
- **Использование**: Если нужно разнообразие тем, увеличьте до `0.5-1.0`

---

### ⚡ Streaming (потоковая передача)

#### `streaming`
- **Описание**: Включить потоковую передачу ответов
- **Тип**: Boolean
- **Значения**:
  - `true` - ✅ **Рекомендуется** - Ответ приходит по частям (быстрее видно начало)
  - `false` - Ждать полный ответ (медленнее, но проще)
- **По умолчанию**: `true`
- **Преимущества streaming**:
  - Пользователь видит ответ сразу, не ждет
  - Лучший UX для чата
  - Можно озвучивать по мере поступления

---

### ⏱️ Таймауты и повторы

#### `timeout`
- **Описание**: Максимальное время ожидания ответа (в секундах)
- **Тип**: Integer
- **Рекомендации**:
  - `30` - Для быстрых моделей (gpt-3.5-turbo)
  - `60` - ✅ **По умолчанию** - Для GPT-4
  - `120` - Для очень длинных запросов
- **По умолчанию**: `60`

#### `max-retries`
- **Описание**: Количество попыток при ошибке
- **Тип**: Integer
- **Рекомендации**:
  - `1` - Одна попытка
  - `3` - ✅ **По умолчанию** - Три попытки
  - `5` - Для нестабильного интернета
- **По умолчанию**: `3`

---

## 🎯 Рекомендуемые конфигурации

### Для обучения (точность важна)
```yaml
openai:
  model: gpt-4-turbo-preview
  max-tokens: 2000
  temperature: 0.3
  top-p: 0.9
  frequency-penalty: 0.5
  presence-penalty: 0.3
  streaming: true
```

### Для творческих задач
```yaml
openai:
  model: gpt-4-turbo-preview
  max-tokens: 3000
  temperature: 0.9
  top-p: 1.0
  frequency-penalty: 0.0
  presence-penalty: 0.5
  streaming: true
```

### Для быстрых ответов (экономия)
```yaml
openai:
  model: gpt-3.5-turbo
  max-tokens: 1000
  temperature: 0.7
  streaming: true
  timeout: 30
```

### Для детальных объяснений
```yaml
openai:
  model: gpt-4-turbo-preview
  max-tokens: 4000
  temperature: 0.5
  streaming: true
  timeout: 120
```

---

## 💰 Стоимость моделей (примерно)

| Модель | Input (за 1M токенов) | Output (за 1M токенов) |
|--------|----------------------|------------------------|
| gpt-4-turbo-preview | $10 | $30 |
| gpt-4 | $30 | $60 |
| gpt-4o | $5 | $15 |
| gpt-4o-mini | $0.15 | $0.6 |
| gpt-3.5-turbo | $0.5 | $1.5 |

**Пример расчета:**
- 1 сессия = ~2000 токенов input + 2000 токенов output = 4000 токенов
- GPT-4-turbo: $0.01 + $0.06 = **$0.07 за сессию**
- GPT-3.5-turbo: $0.002 + $0.003 = **$0.005 за сессию**

---

## 🔧 Настройка через переменные окружения

Все параметры можно переопределить через env переменные:

```bash
export OPENAI_MODEL=gpt-4o-mini
export OPENAI_MAX_TOKENS=1500
export OPENAI_TEMPERATURE=0.5
export OPENAI_STREAMING=true
```

---

## 📊 Мониторинг использования

### Проверка баланса OpenAI
1. Зайдите на https://platform.openai.com/usage
2. Посмотрите текущий баланс и использование

### Логирование в приложении
В логах backend вы увидите:
```
OpenAI Settings: model=gpt-4-turbo-preview, maxTokens=2000, temperature=0.7, streaming=true
```

---

## 🚀 Streaming Implementation

Streaming реализован через метод `chatStream()` в `OpenAIService`:

```kotlin
openAIService.chatStream(
    systemPrompt = prompt,
    conversationHistory = history,
    userMessage = message,
    onChunk = { chunk ->
        // Отправляем chunk клиенту через WebSocket
        sendToClient(chunk)
    }
)
```

---

## ⚠️ Ограничения

1. **Максимум токенов**: Зависит от модели
   - GPT-4: до 8192 токенов контекста
   - GPT-3.5: до 4096 токенов контекста

2. **Rate Limits**: OpenAI ограничивает количество запросов
   - Free tier: 3 запроса/минуту
   - Paid: зависит от плана

3. **Таймауты**: Длинные ответы могут превысить timeout

---

## 🎓 Примеры использования

### Короткие быстрые ответы
```yaml
max-tokens: 500
temperature: 0.3
model: gpt-3.5-turbo
```

### Детальные объяснения
```yaml
max-tokens: 4000
temperature: 0.5
model: gpt-4-turbo-preview
```

### Креативные диалоги
```yaml
max-tokens: 2000
temperature: 0.9
presence-penalty: 0.6
```

---

## 📝 Примечания

- **Temperature vs Top-p**: Используйте один из них, не оба
- **Streaming**: Всегда рекомендуется для лучшего UX
- **Модели**: GPT-4 дороже, но умнее. GPT-3.5 быстрее и дешевле
- **Токены**: Следите за лимитами, длинные сессии могут быть дорогими

---

**Обновлено**: 2024-12-03

